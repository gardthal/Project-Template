# Copy this file to .env and fill in real per-project values.
# Never commit .env or any file containing real secrets.
# Each project should have its own unique keys when possible.

# Example API key. Leave blank in this committed template.
OPENAI_API_KEY=

# Optional project environment label: development, test, staging, production.
APP_ENV=development
